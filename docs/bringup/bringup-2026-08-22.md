# Bring-up report 2026-08-22

Transport: Wi-Fi / HTTP, host `192.168.4.1` (stock firmware).

Confirmed: 0 · Differs: 0 · Skipped: 0 · Errors: 1

| Assumption | Check | Outcome | Note |
|---|---|---|---|
| D1 | Robot answers on HTTP | **error** | cannot reach robot at http://192.168.4.1/control?var=move&val=3&cmd=0: <urlopen error [WinError 10013] Der Zugriff auf einen Socket war aufgrund der Zugriffsrechte des Sockets unzulässig>
  Your PC is on the right subnet (192.168.4.2), so the network side looks fine. The robot may still be booting, or its web server is not up yet -- try http://192.168.4.1/ in a browser. |

## What to do with this

Transfer every `confirmed` row into ASSUMPTIONS.md as `verified <date>`, and every `differs` row as `wrong` with the correction — do not delete the original claim.
