# Simulation assets

Placeholder for milestone M1: the MuJoCo model (MJCF) of the WAVEGO, built from
the linkage dimensions in `src/robodog/kinematics/constants.py`, plus any meshes.

Masses, inertias and the center of mass are unknown so far — see ASSUMPTIONS E1.
Higher-fidelity meshes may later come from the parametric CadQuery leg model
(ROADMAP M6).
